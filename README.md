# Ownit

## Executar o sistema localmente

Para executar o sistema localmente usando docker, use os comandos a seguir.
```bash
# Crie o .env
cp .env.example .env

# Execute o sistema usando o Docker
docker compose up
```