meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: http://localhost:8000/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "aluno@example.com",
    "password": "senha123"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
