meta {
  name: Update StudySession status
  type: http
  seq: 3
}

patch {
  url: http://localhost:8000/sessions/{{study_session_id}}/status
  body: json
  auth: inherit
}

body:json {
  {
    "new_status": "doing"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
